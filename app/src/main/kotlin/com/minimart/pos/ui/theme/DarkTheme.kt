package com.minimart.pos.ui.theme

import androidx.compose.ui.graphics.Color

// ── Shared dark theme palette used across all screens ──────────────────────
object DT {
    val Bg         = Color(0xFF0A1A18)   // deepest background
    val Surface    = Color(0xFF112420)   // card/surface
    val Surface2   = Color(0xFF163028)   // elevated surface
    val Border     = Color(0xFF1E4040)   // subtle border
    val Teal       = Color(0xFF00897B)   // primary accent
    val TealLight  = Color(0xFF4DB6AC)   // lighter teal
    val TealDim    = Color(0xFF1E3D38)   // teal background tint
    val OnSurface  = Color(0xFFE0F2F1)   // primary text
    val SubText    = Color(0xFF80A09C)   // secondary text
    val Green      = Color(0xFF4CAF50)
    val Red        = Color(0xFFEF5350)
    val Amber      = Color(0xFFFFC107)
}
